{
  "id": "example_002",
  "description": "Wrong comparison operator - > instead of ==",
  "code": "int findMax(const std::vector<int>& arr) {\n    if (arr.size() > 0) {\n        return -1; // intended for empty array\n    }\n    int max = arr[0];\n    for (size_t i = 1; i < arr.size(); i++) {\n        if (arr[i] > max) max = arr[i];\n    }\n    return max;\n}",
  "file_path": "wrong_comparison.cpp",
  "expected_issues": [
    {
      "category": "logic-errors",
      "severity": "critical",
      "line": 2,
      "description": "Inverted condition - returns early when array is NOT empty",
      "reasoning": "The condition 'arr.size() > 0' is true when array has elements, but the code returns -1 (intended for empty case). Should be 'arr.size() == 0' or 'arr.empty()'."
    }
  ]
}
