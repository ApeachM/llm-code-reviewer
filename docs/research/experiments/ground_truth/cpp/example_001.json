{
  "id": "example_001",
  "description": "Off-by-one error in loop - uses <= instead of <",
  "code": "std::vector<int> nums = {1, 2, 3, 4, 5};\nint sum = 0;\nfor (int i = 0; i <= nums.size(); i++) {\n    sum += nums[i];\n}\nreturn sum;",
  "file_path": "off_by_one_loop.cpp",
  "expected_issues": [
    {
      "category": "logic-errors",
      "severity": "critical",
      "line": 3,
      "description": "Off-by-one error: loop condition uses <= instead of <",
      "reasoning": "Loop iterates with i <= nums.size(), which allows i to equal 5 (the size). This causes out-of-bounds access at nums[5] since valid indices are 0-4."
    }
  ]
}
