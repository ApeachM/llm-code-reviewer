{
  "id": "example_011",
  "description": "Comment contradicts code behavior",
  "code": "class DataProcessor {\npublic:\n    // Returns the sum of all positive numbers\n    int calculate(const std::vector<int>& nums) {\n        int result = 0;\n        for (int n : nums) {\n            if (n < 0) result += n;  // Adds negative numbers!\n        }\n        return result;\n    }\n};",
  "file_path": "comment_code_mismatch.cpp",
  "expected_issues": [
    {
      "category": "semantic-inconsistency",
      "severity": "high",
      "line": 7,
      "description": "Code behavior contradicts comment documentation",
      "reasoning": "Comment on line 3 says 'sum of all positive numbers' but condition on line 7 checks 'n < 0' and adds negative numbers. Either the comment or the condition is wrong."
    }
  ]
}
