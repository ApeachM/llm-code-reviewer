{
  "id": "example_012",
  "description": "Variable name misleads about content",
  "code": "std::vector<std::string> getUserNames(Database& db) {\n    std::vector<std::string> userEmails;\n    auto results = db.query(\"SELECT email FROM users\");\n    for (const auto& row : results) {\n        userEmails.push_back(row[\"email\"]);\n    }\n    return userEmails;  // Returns emails, not names!\n}",
  "file_path": "misleading_variable_name.cpp",
  "expected_issues": [
    {
      "category": "semantic-inconsistency",
      "severity": "medium",
      "line": 1,
      "description": "Function name suggests names but returns emails",
      "reasoning": "Function is named 'getUserNames' but queries and returns emails (line 3-5). Variable is correctly named 'userEmails' but function name is misleading. Should rename to 'getUserEmails'."
    }
  ]
}
