{
  "id": "example_016",
  "description": "Unhandled error case in switch statement",
  "code": "enum class Status { OK, ERROR, PENDING, CANCELLED };\n\nstd::string statusToString(Status s) {\n    switch (s) {\n        case Status::OK: return \"OK\";\n        case Status::ERROR: return \"Error\";\n        case Status::PENDING: return \"Pending\";\n        // Missing: case Status::CANCELLED\n    }\n    return \"\";  // Silent failure for CANCELLED\n}",
  "file_path": "unhandled_enum_case.cpp",
  "expected_issues": [
    {
      "category": "edge-case-handling",
      "severity": "high",
      "line": 4,
      "description": "Missing case for Status::CANCELLED in switch",
      "reasoning": "Switch statement handles OK, ERROR, PENDING but not CANCELLED. If CANCELLED is passed, function silently returns empty string. Should handle all enum cases or add default with assertion."
    }
  ]
}
