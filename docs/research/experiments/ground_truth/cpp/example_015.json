{
  "id": "example_015",
  "description": "Missing boundary check in array access",
  "code": "int getElement(const std::vector<int>& arr, int index) {\n    return arr[index];  // No bounds check!\n}\n\nvoid process() {\n    std::vector<int> data = {1, 2, 3};\n    int x = getElement(data, 10);  // Out of bounds!\n}",
  "file_path": "missing_bounds_check.cpp",
  "expected_issues": [
    {
      "category": "edge-case-handling",
      "severity": "high",
      "line": 2,
      "description": "No bounds checking before array access",
      "reasoning": "Function accesses arr[index] without validating index is within bounds. Caller on line 7 passes index 10 for a 3-element vector. Use arr.at(index) for bounds checking or add explicit validation."
    }
  ]
}
