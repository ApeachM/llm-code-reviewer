{
  "id": "example_014",
  "description": "Missing null check before dereferencing",
  "code": "struct Node {\n    int value;\n    Node* next;\n};\n\nint getNextValue(Node* node) {\n    return node->next->value;  // What if node or next is null?\n}",
  "file_path": "missing_null_check.cpp",
  "expected_issues": [
    {
      "category": "edge-case-handling",
      "severity": "critical",
      "line": 7,
      "description": "Dereferencing potentially null pointers without check",
      "reasoning": "node->next->value dereferences both node and node->next without null checks. If either is null, this causes a crash. Should check: if (node && node->next) before accessing."
    }
  ]
}
