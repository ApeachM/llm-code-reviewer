{
  "id": "example_018",
  "description": "Clean code - consistent naming and behavior",
  "code": "class UserRepository {\npublic:\n    void addUser(const User& user) {\n        users_.push_back(user);\n    }\n\n    void removeUser(const std::string& id) {\n        users_.erase(\n            std::remove_if(users_.begin(), users_.end(),\n                [&id](const User& u) { return u.id == id; }),\n            users_.end());\n    }\n\n    std::optional<User> findUserById(const std::string& id) const {\n        auto it = std::find_if(users_.begin(), users_.end(),\n            [&id](const User& u) { return u.id == id; });\n        if (it != users_.end()) return *it;\n        return std::nullopt;\n    }\n\nprivate:\n    std::vector<User> users_;\n};",
  "file_path": "clean_consistent_naming.cpp",
  "expected_issues": []
}
