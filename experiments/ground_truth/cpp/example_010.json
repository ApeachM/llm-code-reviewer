{
  "id": "example_010",
  "description": "Function name contradicts behavior",
  "code": "class UserManager {\npublic:\n    void addUser(const std::string& name) {\n        users_.erase(std::remove(users_.begin(), users_.end(), name), users_.end());\n    }\n\nprivate:\n    std::vector<std::string> users_;\n};",
  "file_path": "name_behavior_mismatch.cpp",
  "expected_issues": [
    {
      "category": "semantic-inconsistency",
      "severity": "critical",
      "line": 3,
      "description": "Function named 'addUser' actually removes users",
      "reasoning": "Function is named 'addUser' but uses erase-remove idiom which deletes matching users. Either the name is wrong (should be removeUser) or the implementation is wrong."
    }
  ]
}
