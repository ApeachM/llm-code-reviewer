{
  "id": "example_007",
  "description": "Ignoring return value of critical function",
  "code": "void processData(const std::string& filename) {\n    std::ifstream file(filename);\n    file.read(buffer, sizeof(buffer));\n    // Ignoring return value - don't know if read succeeded\n    processBuffer(buffer);\n}",
  "file_path": "ignored_return_value.cpp",
  "expected_issues": [
    {
      "category": "api-misuse",
      "severity": "high",
      "line": 3,
      "description": "Return value of read() ignored",
      "reasoning": "file.read() returns the stream reference indicating success/failure, but it's not checked. If read fails, processBuffer receives uninitialized or partial data."
    }
  ]
}
