# 🚀 Quick Start Guide - 실전 사용법

C++ LLM Reviewer를 **5분 안에** 사용하는 방법입니다.

---

## 📦 사전 준비 (이미 완료된 상태)

✅ Python 3.12+ 설치됨
✅ Ollama 설치됨
✅ `deepseek-coder:33b-instruct` 모델 다운로드됨
✅ 프로젝트 dependencies 설치됨

---

## 🎯 3가지 주요 사용 방법

### 1️⃣ **단일 파일 분석** (가장 기본)

```bash
# 기본 사용법
python -m cli.main analyze file src/main.cpp

# 결과를 markdown 파일로 저장
python -m cli.main analyze file src/main.cpp --output report.md

# 다른 모델 사용
python -m cli.main analyze file src/main.cpp --model qwen2.5:14b
```

**출력 예시**:
```
Analyzing file: src/main.cpp
Model: deepseek-coder:33b-instruct

Found 3 issue(s):

🔴 Line 12 [memory-safety] Memory leak - dynamically allocated pointer never deleted
  Pointer allocated with 'new' on line 12 but no corresponding 'delete'.
  Memory leak on every execution.

🟡 Line 25 [performance] Unnecessary copy in loop
  Vector passed by value in loop. Use const reference to avoid copies.

🟠 Line 34 [modern-cpp] Use std::unique_ptr instead of raw pointer
  Manual memory management. Use std::unique_ptr for automatic cleanup.
```

**분석 시간**: 약 8-10초 (few-shot-5 technique)

---

### 2️⃣ **디렉토리 전체 분석**

```bash
# 전체 디렉토리 분석 (recursive)
python -m cli.main analyze dir src/

# 특정 디렉토리만 (non-recursive)
python -m cli.main analyze dir src/ --no-recursive

# 결과를 파일로 저장
python -m cli.main analyze dir src/ --output directory_report.md
```

**출력 예시**:
```
Analyzing directory: src/
Model: deepseek-coder:33b-instruct
Recursive: True

       Analysis Summary
┏━━━━━━━━━━━━━━━━━┳━━━━━━━┓
┃ Metric          ┃ Value ┃
┡━━━━━━━━━━━━━━━━━╇━━━━━━━┩
│ Files Analyzed  │ 8     │
│ Total Issues    │ 12    │
│ Critical Issues │ 3     │
│ High Issues     │ 5     │
│ Medium Issues   │ 3     │
│ Low Issues      │ 1     │
└─────────────────┴───────┘

Issues by Category:
  memory-safety: 5
  performance: 3
  modern-cpp: 3
  concurrency: 1
```

**분석 시간**: 파일당 8-10초 (8개 파일 = 약 70초)

---

### 3️⃣ **Pull Request 분석** (가장 유용!)

```bash
# 현재 브랜치 vs main 비교
python -m cli.main analyze pr --base main --head HEAD --output pr-review.md

# 특정 브랜치 비교
python -m cli.main analyze pr --base develop --head feature-branch

# 다른 repo 분석
python -m cli.main analyze pr --repo /path/to/repo --base main
```

**출력 예시**:
```
Analyzing PR: main...feature-branch
Repository: .
Model: deepseek-coder:33b-instruct

Analyzed 3 changed file(s)
Found 4 issue(s)

📄 feature.cpp:
  🔴 Line 42 Memory leak in new feature
  🟡 Line 58 Use std::unique_ptr instead of raw pointer

📄 utils.cpp:
  🟠 Line 15 Unnecessary copy in loop

PR review saved to: pr-review.md
💡 Tip: Copy this markdown to your PR comment!
```

**생성된 markdown**:
```markdown
## Code Analysis Results

🤖 Analyzed 3 file(s) using LLM-powered analysis

**Found 4 issue(s)** (1 critical ⚠️)

### 📄 feature.cpp

🔴 **Line 42** [memory-safety] Memory leak in new feature
> Pointer allocated but never freed. Add delete or use smart pointer.

🟡 **Line 58** [modern-cpp] Use std::unique_ptr instead of raw pointer
> Manual memory management. Use std::unique_ptr for automatic cleanup.

---

_🤖 Generated by LLM Framework using few-shot-5 technique (F1: 0.615)_
```

---

## 🎓 고급 사용법

### **실험 실행** (연구 모드)

새로운 prompting technique을 테스트하고 싶을 때:

```bash
# 실험 실행
python -m cli.main experiment run --config experiments/configs/few_shot_5.yml

# 결과 비교
python -m cli.main experiment leaderboard
```

**Leaderboard 출력**:
```
              Experiment Results
┏━━━━━━━━━━━━━━━━━━┳━━━━━━━━┳━━━━━━━━━━━┳━━━━━━━━┓
┃ Experiment       ┃ F1     ┃ Precision ┃ Recall ┃
┡━━━━━━━━━━━━━━━━━━╇━━━━━━━━╇━━━━━━━━━━━╇━━━━━━━━┩
│ hybrid           │ 0.634  │ 0.650     │ 0.619  │
│ few_shot_5       │ 0.615  │ 0.667     │ 0.571  │
│ few_shot_3       │ 0.588  │ 0.769     │ 0.476  │
│ chain_of_thought │ 0.571  │ 0.571     │ 0.571  │
│ zero_shot        │ 0.526  │ 0.588     │ 0.476  │
└──────────────────┴────────┴───────────┴────────┘
```

---

## 🛠️ 실전 Workflow

### **Workflow 1: 로컬 개발 중**

```bash
# 코드 작성 중 파일 체크
vim src/feature.cpp
python -m cli.main analyze file src/feature.cpp

# 문제 발견 → 수정 → 재체크
vim src/feature.cpp
python -m cli.main analyze file src/feature.cpp

# ✅ 이슈 없으면 commit
git add src/feature.cpp
git commit -m "Add new feature"
```

---

### **Workflow 2: PR 작성 전**

```bash
# Feature 브랜치에서 작업 완료
git checkout feature-new-api

# PR 리뷰 시뮬레이션
python -m cli.main analyze pr --base main --head HEAD --output review.md

# 리뷰 확인
cat review.md

# 문제 수정 후 다시 체크
python -m cli.main analyze pr --base main --head HEAD

# ✅ 깨끗하면 PR 생성
gh pr create --title "Add new API" --body-file review.md
```

---

### **Workflow 3: CI/CD 통합**

**Option A: GitHub Actions**

`.github/workflows/code-review.yml` 생성:

```yaml
name: AI Code Review

on:
  pull_request:
    branches: [main]

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install Dependencies
        run: pip install -e .

      - name: Setup Ollama
        run: |
          curl https://ollama.ai/install.sh | sh
          ollama pull deepseek-coder:33b-instruct

      - name: Run Analysis
        run: |
          python -m cli.main analyze pr \
            --base ${{ github.base_ref }} \
            --head ${{ github.head_ref }} \
            --output review.md

      - name: Post PR Comment
        uses: actions/github-script@v6
        with:
          script: |
            const fs = require('fs');
            const review = fs.readFileSync('review.md', 'utf8');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: review
            });
```

**Option B: Pre-commit Hook**

`.git/hooks/pre-commit` 생성:

```bash
#!/bin/bash
# 자동으로 staged C++ 파일 분석

STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep '\\.cpp$\|\\.h$')

if [ -n "$STAGED_FILES" ]; then
    echo "🔍 Running C++ code analyzer..."

    for file in $STAGED_FILES; do
        python -m cli.main analyze file "$file" > /dev/null
        if [ $? -ne 0 ]; then
            echo "❌ Issues found in $file"
            python -m cli.main analyze file "$file"
            exit 1
        fi
    done

    echo "✅ Code quality checks passed"
fi
```

실행 권한 부여:
```bash
chmod +x .git/hooks/pre-commit
```

---

## ⚙️ 설정 & 최적화

### **어떤 Technique 사용할까?**

| 상황 | 추천 Technique | 이유 |
|-----|---------------|------|
| **일반 PR 리뷰** | Few-shot-5 (default) | 최고의 speed/accuracy 균형 |
| **Critical PR** (보안, 코어) | Hybrid | +3.1% F1, modern-cpp 지원 |
| **대량 파일 분석** (100+ files) | Few-shot-3 | 20% 빠름, 토큰 절약 |
| **Modern C++ 코드베이스** | Hybrid 또는 CoT | Modern-cpp 이슈 탐지 |
| **빠른 체크** | Zero-shot | 가장 빠름 (7초), 정확도 낮음 |

**Technique 변경** (현재는 코드 수정 필요):
```python
# plugins/production_analyzer.py 수정
# 기본: few-shot-5
self.technique = FewShotTechnique(...)

# Hybrid로 변경
self.technique = HybridTechnique(...)
```

---

### **모델 선택**

```bash
# 기본 (권장): deepseek-coder:33b-instruct (18GB, 느림, 정확)
python -m cli.main analyze file src/main.cpp

# 빠른 모델: qwen2.5:14b (8GB, 빠름, 약간 낮은 정확도)
python -m cli.main analyze file src/main.cpp --model qwen2.5:14b

# 최고 성능: qwen2.5:72b (47GB, 매우 느림, 최고 정확도)
python -m cli.main analyze file src/main.cpp --model qwen2.5:72b
```

**현재 설치된 모델**:
- ✅ `deepseek-coder:33b-instruct` (18GB)
- ✅ `qwen2.5:14b` (9GB)
- ✅ `qwen2.5:72b` (47GB)
- ✅ `starcoder2:15b` (9GB)

---

## 🚨 문제 해결

### **문제 1: "File not analyzed (filtered out)"**

**원인**: 파일명에 "test"가 포함되거나 제외 디렉토리에 있음

**해결**:
```bash
# 파일명에 'test' 있으면 필터링됨
# example_test.cpp ❌
# example_code.cpp ✅

# 제외 디렉토리: third_party/, vendor/, external/, node_modules/
```

**일시적 해결**: 파일명 변경 또는 일시적으로 복사

---

### **문제 2: Ollama 연결 에러**

**에러**: `ConnectionError: Cannot connect to Ollama`

**해결**:
```bash
# Ollama 실행 확인
ollama list

# Ollama 실행 안 되어있으면
ollama serve

# 다른 터미널에서 다시 시도
python -m cli.main analyze file src/main.cpp
```

---

### **문제 3: 분석이 너무 느림**

**현재 속도**: 파일당 8-10초 (few-shot-5)

**최적화 방법**:

1. **더 빠른 모델 사용**:
```bash
python -m cli.main analyze file src/main.cpp --model qwen2.5:14b
# 속도: 8초 → 5초
```

2. **Few-shot-3 사용** (코드 수정 필요):
```python
# production_analyzer.py
self.plugin.get_few_shot_examples(num_examples=3)  # 5 → 3
# 속도: 8초 → 6초, 토큰 20% 절약
```

3. **디렉토리 분석 시 non-recursive**:
```bash
python -m cli.main analyze dir src/ --no-recursive
```

---

### **문제 4: 너무 많은 False Positive**

**현재 precision**: 66.7% (33% false positives)

**해결 방법**:

1. **Hybrid technique 사용** (confidence filtering):
   - 코드 수정 필요 (production_analyzer.py)

2. **시스템 프롬프트 조정**:
   - `plugins/cpp_plugin.py`의 system prompt 수정
   - "Be conservative", "Only report clear issues" 강조

3. **Few-shot examples 개선**:
   - False positive 예제를 negative example로 추가

---

## 📊 성능 & 비용

### **속도**
- 파일당: 8-10초 (few-shot-5)
- 100 files: ~13.5분
- 1000 files: ~2.3시간

### **토큰 사용**
- 파일당: ~620 tokens (few-shot-5)
- 100 files: ~62K tokens
- 1000 files: ~620K tokens

### **비용** (Ollama = 무료, OpenAI 참고용)
- Ollama (local): **무료** ✅
- OpenAI gpt-4 (참고): $18.60 per 1000 files

---

## 🎯 추천 사용 패턴

### **초보자 (첫 사용)**
```bash
# 1. 간단한 파일 하나 테스트
python -m cli.main analyze file example_code.cpp

# 2. 결과 확인 후 디렉토리 분석
python -m cli.main analyze dir src/ --output report.md

# 3. 리포트 읽기
cat report.md
```

---

### **일상 사용**
```bash
# 매일 코딩 후
python -m cli.main analyze file src/today_work.cpp

# PR 전
python -m cli.main analyze pr --base main --output review.md
```

---

### **팀 적용**
1. GitHub Actions workflow 추가
2. Pre-commit hook 설정
3. 팀원들에게 로컬 사용법 공유
4. False positive 피드백 수집 → few-shot examples 개선

---

## 🔥 실전 팁

1. **파일명 주의**: "test"가 들어가면 자동 필터링
2. **PR 분석이 최고**: 변경된 파일만 보니까 빠름
3. **Markdown 저장**: `--output` 옵션으로 공유 가능한 리포트
4. **Critical PR엔 Hybrid**: 중요한 PR은 hybrid technique (코드 수정 필요)
5. **결과 신뢰도**: Precision 66.7% → 리뷰 후 수동 검증 필수

---

## 📚 더 알아보기

- **전체 문서**: `README.md`
- **연구 결과**: `docs/PHASE2_FINDINGS.md`, `docs/PHASE4_COMPLETE.md`
- **아키텍처**: `docs/PHASE3_PRODUCTION.md`
- **커스터마이징**: `plugins/cpp_plugin.py` 수정

---

**준비 완료! 이제 바로 사용해보세요** 🚀

```bash
python -m cli.main analyze file your_code.cpp
```
