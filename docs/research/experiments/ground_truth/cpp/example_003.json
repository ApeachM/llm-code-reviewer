{
  "id": "example_003",
  "description": "Boolean logic error - wrong operator in condition",
  "code": "bool isValidRange(int value, int min, int max) {\n    return value >= min || value <= max;\n}",
  "file_path": "boolean_logic_error.cpp",
  "expected_issues": [
    {
      "category": "logic-errors",
      "severity": "high",
      "line": 2,
      "description": "Wrong boolean operator - uses OR instead of AND",
      "reasoning": "Function intends to check if value is within [min, max] range, but uses || (OR) instead of && (AND). Current logic returns true for ANY value since one condition is always true."
    }
  ]
}
