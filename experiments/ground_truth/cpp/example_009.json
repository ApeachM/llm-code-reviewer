{
  "id": "example_009",
  "description": "Getter function modifies internal state",
  "code": "class Counter {\n    int count_ = 0;\n    bool accessed_ = false;\n\npublic:\n    int getCount() {\n        accessed_ = true;  // Side effect in getter!\n        return count_;\n    }\n\n    void increment() { count_++; }\n};",
  "file_path": "getter_side_effect.cpp",
  "expected_issues": [
    {
      "category": "semantic-inconsistency",
      "severity": "medium",
      "line": 7,
      "description": "Getter function has unexpected side effect",
      "reasoning": "Function named 'getCount' implies a read-only operation, but it modifies accessed_ member. Violates principle of least surprise; getters should not have side effects."
    }
  ]
}
