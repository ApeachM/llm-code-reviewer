# LLM 기반 C++ 코드 리뷰어 - 발표 스크립트

**예상 발표 시간: 40분** (Appendix 내용 포함)

---

## 오프닝 (2분)

> 안녕하세요. 오늘 발표드릴 주제는 **"LLM 기반 C++ 코드 리뷰어"**입니다.
>
> 먼저 이 프로젝트가 **무엇을 하는 게 아닌지** 말씀드리겠습니다.
>
> **정적 분석**(clang-tidy)이나 **동적 분석**(sanitizer)이 잡는 버그는 이미 좋은 도구들이 있습니다.
> null pointer, buffer overflow, memory leak 같은 건 그쪽이 더 잘 잡습니다.
>
> 이 프로젝트가 집중하는 건 **"코드 오타로 인한 논리 오류"**입니다.
> - 변수명 오타: `count` 써야 하는데 `cnt` 씀
> - 복사-붙여넣기 실수: 조건문 복사하다가 변수 안 바꿈
> - 의도와 다른 로직: `if (x = 5)` vs `if (x == 5)`
>
> 이런 건 정적 분석기가 잡기 어렵습니다. **코드를 이해하고 의도를 파악**해야 하니까요.
> 그래서 **LLM**을 활용합니다.
>
> 발표는 크게 3파트로 진행합니다.
> - 첫 번째, **왜 이걸 만들었는지** - 배경과 문제
> - 두 번째, **어떻게 동작하는지** - 핵심 기술
> - 세 번째, **결과와 향후 계획**
>
> 참고로 발표 중 상세 설명이 필요한 부분은 **Appendix**를 보여드리겠습니다.

---

## 📌 한눈에 보는 요약 (2분)

> 본격적인 내용에 앞서, 전체 그림을 먼저 보여드리겠습니다.
>
> [다이어그램 가리키며]
>
> **문제**는 두 가지입니다.
> 1. 보안 환경이라 ChatGPT나 Claude 같은 외부 API를 쓸 수 없습니다.
> 2. 그래서 로컬 LLM을 쓰는데, 로컬 LLM은 **인풋이 길어지면 퀄리티가 급격히 떨어집니다**.
>
> **해결책**은 Multi-Stage Pipeline입니다.
> - 긴 파일을 **Chunking**으로 작게 나누고
> - **여러 Worker**가 병렬로 분석한 뒤
> - **결과를 병합**해서 최종 결과를 만듭니다.
>
> **성과**로는 F1 Score가 0.526에서 0.634로 **20% 향상**되었고,
> 향후 Aggregator LLM 등 6가지 기술을 적용해서 **0.80 이상**을 목표로 하고 있습니다.

---

## 1. 프로젝트 배경 및 동기 (6분)

### 1.1 문제 상황 (2분)

> 이 프로젝트는 **"보안 환경에서 LLM을 어떻게 쓸 수 있을까?"**라는 질문에서 시작되었습니다.
>
> 표를 보시면, ChatGPT나 Claude API는 코드가 외부로 나가기 때문에 **보안 정책 위반**입니다.
> GitHub Copilot도 클라우드 기반이라 **내부 네트워크에서 사용 불가**합니다.
>
> 그렇다고 clang-tidy 같은 정적 분석기만 쓰자니 한계가 있습니다.
> 정적 분석기는 **"이런 패턴은 버그다"**라는 규칙이 있어야 잡습니다.
>
> 예를 들어,
> ```cpp
> for (int i = 0; i < n; i++) {
>     for (int j = 0; j < m; i++) {  // j++ 해야 하는데 i++ 함
>         // ...
>     }
> }
> ```
> 이런 **복사-붙여넣기 실수**는 정적 분석기가 못 잡습니다.
> 문법적으로 완벽하거든요. `i++`가 틀린 게 아니라, **의도와 다른 것**이 문제입니다.
>
> **LLM**은 코드를 읽고 **"안쪽 루프면 j를 증가해야 하는데?"**라고 파악할 수 있습니다.
>
> 그래서 **핵심 과제**는:
> "외부 API 없이, 내부에서만 LLM 기반 코드 분석을 수행하는 것"이 되었습니다.

### 1.2 해결 방안: 온프레미스 LLM (1분)

> 해결책은 **온프레미스 LLM**입니다.
>
> [다이어그램 가리키며]
> - **DGX-SPARK**: GPU 서버입니다. 24GB VRAM으로 33B 모델을 돌릴 수 있습니다.
> - **Ollama**: 로컬에서 LLM을 서빙하는 플랫폼입니다.
> - **DeepSeek-Coder 33B**: 코드 특화 오픈소스 모델입니다.
> - 그 위에 **Python Framework**와 **CLI**를 구축했습니다.

### 1.3 로컬 LLM의 현실적 한계 (3분)

> 그런데 여기서 **핵심 문제**가 있습니다.
>
> 로컬 LLM은 **인풋이 조금만 길어져도 출력 퀄리티가 급격히 떨어집니다.**
> 이게 이 프로젝트에서 청킹과 파이프라인이 중요한 이유입니다.
>
> [다이어그램 가리키며]
> - 100줄 정도 짧은 인풋은 **퀄리티가 좋습니다**.
> - 300줄 넘어가면 **퀄리티가 떨어지기 시작**합니다.
> - 500줄 이상은 **심각하게 나빠집니다**.
>
> 구체적인 예시를 보여드리면,
>
> [짧은 인풋 예시 가리키며]
> 50줄짜리 코드에서 null check 없는 버그가 있으면, LLM이 **정확한 라인 번호**와 함께 찾아냅니다.
>
> [긴 인풋 예시 가리키며]
> 그런데 같은 버그가 500줄짜리 파일 중간에 있으면?
> - 라인 번호가 **틀립니다** (312줄인데 287줄이라고 함)
> - **핵심 버그를 놓칩니다**
> - 없는 버그를 있다고 하는 **할루시네이션**이 생깁니다
>
> [표 가리키며]
> 정리하면 이렇습니다. 긴 인풋에서는 라인 번호가 틀리고, 버그를 놓치고, 할루시네이션이 생깁니다.
>
> 그래서 **극복 전략**이 필요합니다.
> - 긴 인풋 문제는 **AST 청킹**으로 작은 단위로 분할
> - 분산 분석의 한계는 **Aggregator LLM**으로 종합
> - 느린 속도는 **병렬 처리**
>
> 이게 바로 **Multi-Stage Pipeline**입니다.

---

## 2. 시스템 아키텍처 (3분)

### 2.1 3-Tier 아키텍처 (2분)

> 전체 시스템 구조를 보여드리겠습니다.
>
> [다이어그램 가리키며]
>
> **Tier 3**은 사용자 인터페이스입니다. CLI로 `analyze file`, `analyze dir`, `analyze pr` 명령을 제공합니다.
>
> **Tier 2**는 도메인 플러그인입니다. 현재는 C++ 플러그인이 있고, 여기에 **5개의 Few-shot 예시**와 **5개의 카테고리**가 정의되어 있습니다.
>
> **Tier 1**은 프레임워크 코어입니다. **TechniqueFactory**가 Zero-shot, Few-shot, Hybrid 등 프롬프팅 기법을 관리합니다.
>
> 오른쪽에 **Support Systems**이 있는데, 이게 핵심입니다.
> - **AST Chunker**: 큰 파일을 함수 단위로 분할
> - **Parallel Analyzer**: 4개 Worker가 병렬 분석
> - **Result Merger**: 결과를 합침
>
> 가운데 초록색 **Orchestrator**가 이 모든 흐름을 제어합니다.
> 파일 크기가 작으면 직접 분석하고, 크면 청킹해서 병렬 분석합니다.

### 2.2 기술 스택 (1분)

> 기술 스택을 간단히 정리하면,
> - 하드웨어는 **DGX-SPARK**, 24GB VRAM입니다.
> - LLM은 **Ollama + DeepSeek-Coder 33B**
> - 프레임워크는 **Python 3.12 + Pydantic**
> - AST 파싱은 **tree-sitter**
> - 병렬 처리는 **ThreadPoolExecutor**를 사용합니다.

---

## 3. 오케스트레이션 (8분)

> 이제 핵심인 **오케스트레이션**을 설명드리겠습니다.
> Orchestrator는 시스템의 **심장부**입니다.

### 3.1 이상적인 파이프라인 (2분)

> [다이어그램 가리키며]
>
> 이상적인 파이프라인은 **3단계**입니다.
>
> **Stage 1: Chunking**
> 긴 소스 코드를 AST Chunker가 **작은 단위로 분할**합니다.
> Chunk 1, 2, 3... N개로 나눕니다.
>
> **Stage 2: LLM Workers**
> 각 Chunk를 **병렬로 분석**합니다.
> Worker 1, 2, 3... 각각이 자기 Chunk만 분석합니다.
> 짧은 인풋이니까 **높은 퀄리티**가 나옵니다.
>
> **Stage 3: Aggregator LLM**
> 여기가 핵심입니다. 분산된 결과를 **LLM이 종합**합니다.
> - 충돌 해결
> - False Positive 필터링
> - Cross-chunk 이슈 탐지
>
> 이렇게 해서 최종 결과가 나옵니다.

### 3.2 현재 구현 vs 향후 설계 (2분)

> 그런데 현재 구현은 아직 **이상적인 상태가 아닙니다**.
>
> [다이어그램 가리키며]
>
> **현재**는 ResultMerger가 **규칙 기반**입니다.
> "같은 라인, 같은 카테고리면 중복"이라는 단순 규칙으로 합칩니다.
>
> **향후**에는 Aggregator LLM이 **지능적으로 종합**합니다.
>
> [표 가리키며]
> 차이점을 보시면,
> - 현재는 Cross-chunk 이슈를 **탐지 못 합니다**
> - 현재는 False Positive를 **걸러내지 못 합니다**
> - 향후에 Aggregator를 도입하면 이 문제들이 해결됩니다.

### 3.3 Aggregator LLM 상세 (2분)

> [Appendix 섹션 3.1 보여주며]
>
> **Aggregator LLM**이 구체적으로 어떻게 동작하는지 보여드리겠습니다.
>
> 현재 ResultMerger는 이렇게 단순합니다:
> ```python
> def merge(results):
>     seen = set()
>     for issue in results:
>         key = (issue.line, issue.category)
>         if key not in seen:
>             seen.add(key)
>             yield issue
> ```
> 같은 라인, 같은 카테고리면 중복 제거. 끝입니다.
>
> 향후 Aggregator LLM은 이런 프롬프트를 받습니다:
> ```
> Worker 1 결과 (lines 1-100):
> - Line 45: memory-safety - "malloc without free"
>
> Worker 2 결과 (lines 80-180):
> - Line 120: free 발견
>
> 다음을 수행하세요:
> 1. Worker 1의 malloc이 Worker 2에서 free되는지 확인
> 2. 중복 이슈 통합
> 3. 오탐 제거
> ```
>
> LLM이 **맥락을 이해하고** 판단합니다.

### 3.4 Cross-chunk 이슈 (2분)

> [다이어그램 가리키며]
>
> **Cross-chunk 이슈**가 뭔지 설명드리겠습니다.
>
> Chunk 1에 `malloc`이 있고, Chunk 2에 `free`가 있다고 합시다.
>
> Worker 1은 "malloc 했는데 free가 없네?" → 메모리 릭으로 보고
> Worker 2는 "free 했는데 malloc이 어디있지?" → 이상하다고 보고
>
> **현재** 규칙 기반에서는 둘 다 버그로 보고합니다. → **False Positive**
>
> **향후** Aggregator가 있으면?
> "아, malloc-free 쌍이구나" → **정상 판정**
>
> [표 가리키며]
> 더 많은 예시:
> - `lock`/`unlock` 쌍
> - 변수 초기화/사용 쌍
> - 리소스 획득/해제 쌍
>
> 이런 것들이 다 Cross-chunk 이슈입니다.

---

## 4. 프롬프팅 전략 (7분)

> LLM 성능은 **어떻게 질문하느냐**에 크게 좌우됩니다.
> 5가지 기법을 실험해서 최적 전략을 찾았습니다.

### 4.1 평가 지표 설명 (2분)

> [Appendix 섹션 1 보여주며]
>
> 먼저 **평가 지표**를 설명드리겠습니다.
>
> [TP/FP/FN/TN 표 가리키며]
> - **TP** (True Positive): 실제 버그를 버그로 탐지 ✅
> - **FP** (False Positive): 버그 아닌 걸 버그로 탐지 (오탐)
> - **FN** (False Negative): 실제 버그를 놓침 (미탐)
>
> **Precision** = TP / (TP + FP)
> "탐지한 것 중 진짜 버그 비율" - **오탐이 적으면 높음**
>
> **Recall** = TP / (TP + FN)
> "실제 버그 중 탐지한 비율" - **미탐이 적으면 높음**
>
> **F1 Score** = 2 × (P × R) / (P + R)
> "둘의 조화평균" - **둘 다 높아야 높음**
>
> 왜 조화평균이냐면,
> - Precision 0.9, Recall 0.1 → 산술평균 0.5, **F1 0.18**
> - Precision 0.7, Recall 0.7 → 산술평균 0.7, **F1 0.70**
>
> 한쪽만 높으면 안 됩니다.

### 4.2 기법별 성능 비교 (2분)

> [Appendix 섹션 2 보여주며]
>
> 이제 프롬프팅 기법을 비교해보겠습니다.
>
> **Zero-shot**: 예시 없이 바로 질문.
> ```
> 당신은 C++ 코드 리뷰 전문가입니다.
> 아래 코드를 분석하여 버그를 찾으세요.
> ```
> F1 0.526으로 **기준선**입니다.
>
> **Few-shot**: 예시를 보여주고 따라하게 합니다.
>
> ```
> ## 예시 1:
> 입력: [코드]
> 출력: {"issues": [...]}
> 
> ## 분석할 코드:
> [실제 코드]
> ```
> 예시 5개가 **최적점**입니다. F1 0.615.
>
> **Chain-of-Thought**: "단계별로 생각해봐"
> ```
> 1. 먼저 코드가 무엇을 하는지 파악
> 2. 각 변수의 용도 확인
> 3. 잠재적 버그 식별
> ```
> F1 0.571인데, **modern-cpp에서 압도적**입니다.

### 4.3 Hybrid 기법 상세 (2분)

> [Appendix 섹션 2.4 보여주며]
>
> 재미있는 발견이 있었습니다.
>
> [표 가리키며]
> | 카테고리 | Few-shot-5 | Chain-of-Thought |
> |----------|------------|------------------|
> | memory-safety | 0.800 | 0.833 |
> | performance | 0.800 | 0.500 |
> | security | 1.000 | 0.667 |
> | **modern-cpp** | **0.000** | **0.727** |
>
> Few-shot이 대부분 잘 하는데, **modern-cpp는 0.000**입니다. 완전 실패.
> 반면 CoT는 0.727.
>
> 왜냐하면 modern-cpp는 "raw pointer 대신 smart pointer"처럼
> **단계별 추론**이 필요한 패턴이기 때문입니다.
>
> 그래서 **Hybrid**를 만들었습니다:
> - Pass 1: Few-shot으로 일반 버그 탐지
> - Pass 2: CoT로 modern-cpp 탐지
> - Pass 3: 결과 병합
>
> F1 0.634로 **최고 성능**입니다.
> 4배 느리지만 **CI/CD에서 돌릴 거**라 속도는 상관없습니다.
> 그래서 **Hybrid를 기본 기법**으로 사용합니다.

### 4.4 Self-Critique 기법 (1분)

> [Appendix 섹션 4.5 보여주며]
>
> 향후에는 **Self-Critique**도 적용할 예정입니다.
>
> LLM이 자기 결과를 다시 검증합니다:
> ```
> 아래 이슈가 실제 버그인지 검증하세요:
> - Line 45: "null pointer dereference"
>
> 코드를 보니 if (data) 체크가 있네요.
> → False Positive입니다.
> ```
>
> 예상 효과: **Precision +20%**

---

## 5. AST 기반 청킹 (6분)

> 로컬 LLM은 **컨텍스트 길이가 제한**됩니다.
> DeepSeek은 약 4096 토큰인데, 700줄 C++ 파일은 5000 토큰 정도로 한계를 초과합니다.

### 5.1 문제와 해결 (1분)

> [표 가리키며]
>
> **단순 줄 분할**은 함수 중간에 잘려서 문맥이 손실됩니다.
>
> **AST 청킹**은 함수, 클래스 단위로 나눠서 **의미 단위를 보존**합니다.
> 현재 이 방식을 사용 중입니다.
>
> **Semantic 청킹**은 타입 정보, 호출 관계까지 고려하는 건데,
> clangd가 필요하고 느려서 향후 과제입니다.

### 5.2 tree-sitter vs clangd (2분)

> [Appendix 섹션 5.1 보여주며]
>
> 청킹에 **tree-sitter vs clangd** 중 뭘 쓸지 비교해봤습니다.
>
> [표 가리키며]
>
> | 특성 | tree-sitter | clangd |
> |------|-------------|--------|
> | 속도 | 10ms | 1-2초 |
> | 의존성 | 없음 | compile_commands.json |
> | 타입 정보 | ❌ | **✅** |
> | 호출 관계 | ❌ | **✅** |
>
> 논리 오류 탐지에 clangd가 도움이 될까요?
>
> **YES**입니다.
> - 함수 A가 함수 B를 호출하면, **함께 청킹**할 수 있음
> - `count` vs `cnt` 변수가 다르다는 **타입 정보** 제공
> - 호출 관계를 알면 **관련 코드를 묶어서** 분석 가능
>
> 속도는 100배 차이나지만 (10ms vs 1-2초),
> 어차피 **CI/CD에서 돌릴 거**라 속도는 중요하지 않습니다.
>
> 그래서 향후에는 **clangd 기반 Semantic 청킹**으로 전환할 계획입니다.
> CMake 프로젝트라 compile_commands.json이 이미 있으니까요.

### 5.3 병렬 분석 (2분)

> [다이어그램 가리키며]
>
> 4개 Chunk가 있으면, **4개 Worker**가 동시에 분석합니다.
> 각각 LLM 호출에 8초씩 걸린다고 하면...
>
> - **순차 처리**: 4 × 8 = 32초
> - **병렬 처리**: 약 10초 ← **3.2배 빠름**
>
> ResultMerger에서는:
> 1. 결과 수집
> 2. 라인 번호 조정 (청크 시작 라인 더함)
> 3. 중복 제거 (같은 라인, 같은 카테고리)
> 4. 라인 번호 정렬
>
> 최종 결과가 나옵니다.

### 5.4 향후 개선: Sliding Window (1분)

> [Appendix 섹션 3.2 보여주며]
>
> 현재 청킹의 문제는 **경계에서 이슈를 놓치는 것**입니다.
>
> ```
> 현재:   [1-100] [101-200]  ← 100-101 경계에서 놓침
> 향후:   [1-120] [81-200]   ← 20줄 겹침으로 해결
> ```
>
> **Sliding Window Overlap**을 적용하면
> 경계 이슈를 **80% 탐지**할 수 있습니다.

---

## 6. 실험 및 검증 (4분)

> 모든 기법은 **Ground Truth 데이터셋**으로 객관적으로 평가했습니다.

### 6.1 실험 인프라 (1.5분)

> [다이어그램 가리키며]
>
> **Ground Truth**는 20개 C++ 예제입니다.
> 각 예제에 "여기가 버그다"라고 **수동으로 라벨링**했습니다.
>
> [Appendix 섹션 6.1 보여주며]
>
> 카테고리 분포:
> - memory-safety 5개 (25%)
> - modern-cpp 4개 (20%)
> - performance 3개 (15%)
> - security 2개, concurrency 2개, clean-code 3개

### 6.2 실험 결과 (1.5분)

> [Appendix 섹션 6.2 보여주며]
>
> 각 기법의 상세 결과입니다.
>
> **Zero-shot**:
> ```yaml
> precision: 0.625
> recall: 0.455
> f1_score: 0.526
> ```
> Recall이 낮음 → 버그를 많이 놓침
>
> **Few-shot-5**:
>
> ```yaml
> precision: 0.667
> recall: 0.571
> f1_score: 0.615
> ```
> 대부분 카테고리에서 우수, modern-cpp만 실패
>
> **Hybrid**:
> ```yaml
> precision: 0.684
> recall: 0.591
> f1_score: 0.634
> ```
> 최고 F1, 모든 카테고리 커버

### 6.3 청킹 성능 (1분)

> [Appendix 섹션 6.3 보여주며]
>
> 파일 크기별 청킹 효과:
>
> | 파일 크기 | 청킹 | F1 |
> |-----------|------|-----|
> | 100줄 | 불필요 | 0.615 |
> | 300줄 | 2 chunks | 0.600 |
> | 700줄 | 4 chunks | 0.580 |
>
> 청킹이 많아지면 **F1이 소폭 하락**합니다.
> Cross-chunk 이슈 때문입니다.
> → **Aggregator LLM**으로 해결 예정

---

## 7. 향후 계획 (5분)

> 이 프로젝트는 **아직 완료되지 않았습니다**.

### 7.1 로드맵 (1.5분)

> [다이어그램 가리키며]
>
> **완료된 것** (Phase 0-5):
> - 5가지 프롬프팅 기법
> - AST 청킹
> - CLI 도구
> - C++ 플러그인
> - 규칙 기반 ResultMerger
>
> **최우선** (Phase 6): **Aggregator LLM**
>
> **단기**: Ground Truth 확장, Self-Critique
>
> **중기**: Dynamic Few-shot, Semantic 청킹
>
> **장기**: 새 언어 플러그인, CI/CD 통합

### 7.2 6가지 핵심 기술 (2분)

> [Appendix 섹션 3, 4, 5 보여주며]
>
> 향후 적용할 **6가지 기술**을 정리하면:
>
> 1. **Aggregator LLM** - 정확도 +25%
>    Worker 결과를 LLM이 종합, Cross-chunk 탐지
>
> 2. **Sliding Window Overlap** - 경계 이슈 80%↓
>    청크 겹침으로 경계 문제 해결
>
> 3. **Function Signature Context** - 맥락 +30%
>    다른 함수 시그니처를 컨텍스트로 제공
>
> 4. **Hierarchical Summarization** - Cross-chunk 50%↓
>    각 청크 요약을 공유하여 전체 맥락 파악
>
> 5. **Dynamic Few-shot (RAG)** - 정확도 +15%
>    입력과 유사한 예시를 동적 선택
>
> 6. **Self-Critique** - Precision +20%
>    LLM이 자기 결과 검증

### 7.3 이상적인 파이프라인 (1분)

> [다이어그램 가리키며]
>
> 6가지 기술을 모두 적용한 **이상적인 파이프라인**:
>
> - Stage 0: **clang-tidy Pre-scan** (확실한 버그 먼저)
> - Stage 1: **겹침 청킹 + 요약** 생성
> - Stage 2: **맥락 포함 Workers** 분석
> - Stage 3: **Aggregator LLM** 종합
>
> 현재는 Stage 1만 있고, 나머지는 향후 과제입니다.

### 7.4 기대 효과 (0.5분)

> [표 가리키며]
>
> | 영역 | 현재 | 목표 |
> |------|------|------|
> | F1 Score | 0.634 | **0.80+** |
> | Cross-chunk | 0% | **80%+** |
> | False Positive | ~30% | **~10%** |

---

## 📊 발표 마무리 (2분)

> 마무리하겠습니다.
>
> **이 프로젝트가 하는 것**:
> - 정적 분석기가 못 잡는 **논리 오류, 오타 버그** 탐지
> - **보안 환경**에서 외부 API 없이 LLM 활용
>
> **핵심 기술**:
> - AST 청킹으로 **짧은 인풋 유지** → 높은 퀄리티
> - 병렬 Workers로 **속도 4배 향상**
> - Hybrid 프롬프팅으로 **모든 카테고리 커버**
>
> **성과**:
> - F1 0.526 → **0.634** (20%↑)
>
> **향후**:
> - Aggregator LLM 등 6가지 기술
> - 목표 F1 **0.80+**
>
> 이상입니다. **질문 있으시면** 말씀해 주세요.

---

## 예상 질문 & 답변

### Q1: "clang-tidy로 논리 오류도 잡을 수 있지 않나요?"

> clang-tidy에는 **bugprone-** 체크가 있긴 합니다.
> 예를 들어 `bugprone-copy-constructor-init` 같은 거요.
>
> 하지만 이건 **미리 정의된 패턴**만 잡습니다.
>
> ```cpp
> // clang-tidy가 잡는 것
> if (x = 5)  // bugprone-assignment-in-if
>
> // clang-tidy가 못 잡는 것
> for (int i = 0; i < n; i++)
>     for (int j = 0; j < m; i++)  // j++ 해야 하는데
> ```
>
> 두 번째 예시는 문법적으로 완벽해서 정적 분석으로 못 잡습니다.
> **의도를 파악**해야 하는데, 그게 LLM의 역할입니다.

### Q2: "clangd로 청킹하면 정말 더 좋은가요?"

> **네, 논리 오류 탐지에 확실히 도움**됩니다.
>
> 장점:
> - 함수 A가 B를 호출하면 **함께 청킹** → 관계 파악 용이
> - 타입 정보로 **같은 이름 다른 변수** 구분
> - 호출 그래프로 **관련 코드 그룹핑**
>
> 단점:
> - 100배 느림 (10ms → 1-2초)
> - compile_commands.json 필요
>
> 하지만 **CI/CD에서 돌리기 때문에 속도는 중요하지 않습니다**.
> CMake 프로젝트라 compile_commands.json도 이미 있고요.
>
> 그래서 향후에는 **clangd 기반**으로 전환할 계획입니다.

### Q3: "F1 0.634가 실용적인가요?"

> 솔직히 **단독 사용하기엔 부족**합니다.
> 36%가 오탐이거나 미탐입니다.
>
> 하지만 **보조 도구**로는 유용합니다:
> - 리뷰어가 놓칠 수 있는 버그를 **사전에 제안**
> - security 카테고리는 **F1 1.000**으로 신뢰도 높음
>
> 0.80에 도달하면 실용적 수준이 됩니다.

### Q4: "Aggregator LLM 속도 저하는?"

> N+1번 LLM 호출이 됩니다.
> Aggregator는 결과 텍스트만 보므로 상대적으로 빠르고,
> 예상 추가 시간은 2-3초 정도입니다.
>
> 하지만 **CI/CD에서 돌리기 때문에 속도는 중요하지 않습니다**.
> **정확도가 최우선**이라 Aggregator LLM 도입은 확정입니다.

### Q5: "왜 DeepSeek-Coder?"

> 1. **코드 특화** 오픈소스 중 성능 좋음
> 2. **33B**가 24GB VRAM에 들어감
> 3. **라이선스** 상업적 사용 가능
>
> CodeLlama, StarCoder도 검토했지만, C++ 벤치마크에서 DeepSeek이 더 좋았습니다.

---

**끝**
